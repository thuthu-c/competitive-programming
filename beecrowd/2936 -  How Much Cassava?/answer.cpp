#include <bits/stdc++.h>
using namespace std;
#define ll unsigned long long int

int main() {
    ll C, B, BO, M, I; 

    cin >> C >> B >> BO >> M>> I; 
    cout << (C*300) + (B*1500) + (BO*600) + (M*1000) + (I*150) + 225 << endl; 

    return 0;
}